# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:7.10
      
    working_directory: ~/repo

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package.json" }}
          # fallback to using the latest cache if no exact match is found
          - v1-dependencies-

      - run: yarn install

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}
        
      # run tests and linter!
      - run: yarn test-coverage
      - run: yarn lint
      - run: yarn build

      - setup_remote_docker:
          docker_layer_caching: true

      - run: docker build -t g2pdes/timeline:latest .
      - run: echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin
      - run: docker push g2pdes/timeline:latest
  
      # install heroku CLI
      - run: docker -v
      - run: wget -qO- https://toolbelt.heroku.com/install.sh | sh
      # login to docker registries (dockerhub + heroku)
      - run: echo $HEROKU_PASSWORD docker login -u $HEROKU_USERNAME --password-stdin registry.heroku.com
      - run: echo $HEROKU_PASSWORD | docker login -u $HEROKU_USERNAME --password-stdin registry.heroku.com

