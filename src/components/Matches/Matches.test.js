import React from "react"
import { shallow } from 'enzyme'
import Matches from './Matches'

{
 /* it('Renderiza todos los componentes', () => {
    const listOfMatches = []
    const matches = shallow(<Matches matches={listOfMatches} />)
    expect(matches.find('.matchLink').length).toBe(0)
    expect(matches.find('h3').text()).toEqual("Unirse a Partidas")
    expect(matches.find('.joinMatchButton').length).toBe(1)
  })

  it('Renderiza todas los partidas', () => {
    const listOfMatches = [
      {id: 1, name:"partida 1", size:3},
      {id: 2, name:"partida 2", size:4},
      {id: 3, name:"partida 3", size:5}
    ]
    const matches = shallow(<Matches matches={listOfMatches} />)
    expect(matches.find('.matchLink').length).toBe(3)
  })*/

  it('Llama a la funcion pasada en las props cuando se clickea el boton')

}